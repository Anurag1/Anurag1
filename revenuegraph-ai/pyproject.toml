[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "revenuegraph-ai"
version = "0.1.0"
description = "Research-to-revenue experimentation MVP"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
